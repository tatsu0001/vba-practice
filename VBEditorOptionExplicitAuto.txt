VBAスクリプトの先頭にOption Explicitを自動で付与する設定

- Option Explicit
  -> 未宣言の変数の使用を許可しない
  -> Explicitなしだと未定義の変数は暗黙的に宣言される
     -> 変数の型などがわかりにくいのでソースの見通しがわるくなる


<< 設定方法 >>

1. VBEを起動する

2. ツール→オプション...→編集タブ
  「変数の宣言を強制する」にチェックをいれる→OK

3. 以降モジュールを追加した際に
   ソースの先頭にOption Explicitが自動で設定される

